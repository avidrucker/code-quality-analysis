# Run outputs — every invocation of assess.bb writes here.
# The runner creates the dir on demand; don't track its contents.
reports/

# Per-machine config overrides. See local.example.edn for the template.
local.edn

# Stray temp/junk
*.log
*.tmp
.DS_Store
